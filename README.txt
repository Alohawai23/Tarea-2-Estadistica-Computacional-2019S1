Nombre: Gianni Beninati ROL: 201773508-2

- Consideraciones:
	*Se deben instalar las librerias:
		-Numpy
		-Matplotlib
		-Scipy
	Para hacerlo se puede instalar Anaconda (incluye todas las librerias mencionadas) para el respectivo sistema operativo desde el siguiente link: https://www.anaconda.com/distribution/
	Si se esta utilizando Linux y no se desea instalar Anaconda, tambien se puede usar el comando: pip install <nombre_libreria>.
	*Se añadieron nuevas imagenes a la carpeta img, las cuales se utilizan en la pregunta 1b.
	*Cada respuesta de cada pregunta se divide en una parte de código (bloque de código) y una de texto (bloque Markdown), en donde en la parte de código se encuentra el código de la pregunta respectiva y en la parte de texto, se encuentra el análisis de los resultados.
	*Al correr las simulaciones, se pueden obtener valores distintos a los colocados en las respuestas, pues las simulaciones consideran casos al azar.

- Reglas de ejecución:
	*En el terminal colocar: jupyter notebook (Se asume que ya se encuentra instalado)
	*Se abrirá una nueva pestaña en el navegador en la cual se debe buscar y abrir el archivo .ipynb
	*Para cada bloque de código seleccionar el botón "Run" en la parte superior. (Los bloques se deben correr en orden de arriba hacia abajo)
